export type CredentialsLogin = {
    email: string;
    password: string;
}

export type CredentialsRegister = {
    name: string;
    email: string;
    password: string;
    confirmPassword: string;
}